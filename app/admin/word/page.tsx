import * as React from "react";

export interface IAdminWordPageProps {}

export default function AdminWordPage(props: IAdminWordPageProps) {
  return (
    <div>
      Trang này để cài đặt word cho các ngôn ngữ để tạo ra cái chức năng tự động
      render chữ cho phần spees-test
    </div>
  );
}
